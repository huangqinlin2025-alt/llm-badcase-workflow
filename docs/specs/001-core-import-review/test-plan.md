# SPEC-001：测试计划

- 状态：已冻结
- 版本：1.0
- 日期：2026-09-06

## P0 自动化测试

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| `T-IMP-001` | 导入既有分列 CSV | 解析出 ID、场景、文本和评分方，生成解析报告 |
| `T-IMP-002` | 导入既有数组式 XLSX | 正确拆解数组维度，不执行宏或公式 |
| `T-IMP-003` | 非法扩展名、超限文件、缺关键字段 | 拒绝请求，无候选、无 Outbox、无企微写入 |
| `T-MERGE-001` | 三文件人工/LLM/机检精确合并 | 按 `case_id + side + evaluation_version` 产生一个对齐单元 |
| `T-MERGE-002` | 覆盖不均 | 缺失方为 `unknown`，其余可继续判定 |
| `T-MERGE-003` | 同键分数或文本冲突 | 产生定位问题，冲突单元不投影 |
| `T-AB-001` | 购物神评成对 AB 报告 | 从 `详细配对结果` 拆出 A/B 标准记录，A=`HY-Vision-2.0-instruct`/优化后、B=`quinta_gouwushenping_firstround`/优化前 |
| `T-AB-002` | AB 汇总证据 | 保存配对行号、来源标识、加权总分、差值、胜出方、5 维分数和理由；不依赖文件名 |
| `T-RULE-001` | 四类信号阈值边界 | 验证 `4`、`6`、`1.5`、`2` 边界和 P0/P1/P2 分级 |
| `T-RULE-002` | 规则证据与版本 | 每个候选保存 `core-v1`、阈值快照和证据 |
| `T-DATA-001` | 相同请求幂等重放 | 返回原结果，不重复批次、候选或 Outbox |
| `T-DATA-002` | 同 candidate 内容变化 | 只更新自动字段，保留终态审核字段 |
| `T-REV-001` | 采纳 | 创建审核事件，入队大库回写和小库投影 |
| `T-REV-002` | 驳回 | 创建审核事件，只入队大库状态回写 |
| `T-REV-003` | 并发/重复审核 | 一个成功，过期版本返回冲突 |
| `T-WECOM-001` | 大库 upsert | 模拟既有行时更新目标行，不从第 0 行覆盖 |
| `T-WECOM-002` | 超时与部分失败 | 标记未知/可重试，先对账后重发，不重复追加 |
| `T-WECOM-003` | 服务重启 | 未完成 Outbox 在重启后被继续处理 |
| `T-SEC-001` | 包含 HTML/脚本字段 | API/前端仅文本呈现，无脚本执行 |
| `T-SEC-002` | 受控企微链接 | 非 HTTPS、非白名单、内网或无权限链接全部拒绝 |

## 手工验收

1. 从 GitHub Pages 打开当前 UI，上传三份真实样例，确认页面显示后端返回的真实解析、对齐、判定和同步状态。
2. 对一条候选采纳、另一条驳回；刷新页面和重启服务后确认审核状态仍存在。
3. 在企微大库核验稳定键、自动字段和审核字段；在小库核验仅采纳项存在且不重复。
4. 模拟企微写入异常，确认页面显示“待同步”，然后运行对账/重试直至恢复。
5. 检查日志、页面和 Git 历史，确认没有 Token、内部 ID 或用户原文泄露。

## 发布门槛

所有 P0 自动化测试必须通过；手工主路径与失败恢复各至少完成一次；`acceptance.md` 填写证据；数据库迁移已在空库和升级库执行；发布前已备份 SQLite 数据库。
