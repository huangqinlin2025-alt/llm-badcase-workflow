# Badcase 自动入库工作流研究与概设计划

## 目标
基于 `DELIVERY.md` 和现有代码，确认 Demo 已实现边界、识别生产化缺口，将工作拆分为可交付 issue，定义每个节点的输入/输出/失败语义，完成 20 小时（2.5 天）可落地的技术概设、开发节奏与规范；在用户确认概设前不进入正式开发。

## 需求拆解与约束
核心闭环是“导入多来源评测数据 → 解析/合并/判定 → 幂等写大库 → 人工审核 → 回写大库并写小库”。现有 Demo 已有单文件解析、四类信号与企微双子表写入；P0 缺口是前端审核未真正回写。关键约束包括固定同一企微文档、机器人可写权限、子表容量与写入限流、三路数据覆盖不均、不同表结构，以及不得让高覆盖软违规单独触发入库。

## 查询类型与研究方法
这是“深度优先 + 工程审计”型问题：同一工作流需从业务闭环、数据契约、服务接口、企微一致性与可测试性交叉验证。

将对比三种实施路径：
1. 维持静态前端 + CLI：成本最低但无法安全持久化审核，排除为正式方案。
2. 轻量 FastAPI 单体服务 + 现有纯函数核心：以最小变更补齐 P0，适合 20 小时窗口，作为推荐方案。
3. 直接接入 LangGraph + 持久化状态：编排能力更完整，但超出 P0 窗口，列为后续 P2 演进。

## 信息获取策略
- 代码审计：检查 `badcase_pipeline`、上游 LangGraph 骨架、既有可行性评估和前端调用链，输出真实模块现状、接口不一致处与风险清单。
- 企微集成验证：核查写入、读取、重试、子表定位、批量写入、鉴权与错误处理现状，定义幂等策略和状态机。
- 数据与判定审计：核查导入格式、schema/record/badcase 契约、判定信号、单/多文件合并及测试样例覆盖。
- 微信公众号检索：使用 `wechat-article-search`，检索关键词“企业微信 智能表格 API 幂等 写入 重试”“FastAPI 文件上传 接口规范”，时间范围 `2025-09-05` 至 `2026-09-05`；只将其作为中文实践参考，并与官方 API/现有实现交叉验证，不用其替代代码事实。
- Web 信息：如需补足 FastAPI 或企微当前行为，以官方文档优先；报告会区分已验证事实、设计假设与待用户确认项。

## 并行子任务与预期产出
1. 代码现状审计：输出目录、真实调用链、现有测试/运行能力、与交付文档不一致之处。必要且可独立。
2. 数据链路与判定审计：输出各节点契约、数据质量边界、缺失字段、迁移三类旧信号的影响。必要且可独立。
3. 企微/服务化审计：输出 P0 API、鉴权与输入限制、幂等写入/审核状态转换/失败恢复的建议。必要且可独立。

## 合成方法与交付
先合并子任务的代码证据，形成“已实现 / 缺口 / 风险 / 决策”矩阵；随后与用户沟通业务规则、部署位置、审核责任和成功指标。确认后输出：工作流总图、各阶段图、节点 I/O、issue backlog（优先级/验收标准/依赖）、20 小时排期、技术规范与风险控制。此阶段仅产出设计材料，不修改业务代码。
